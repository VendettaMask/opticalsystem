# UI 设计走查记录

本文保留历史走查、用户反馈和分析 UI 契约演进。当前稳定设计规范见 [UI 设计规范](UI_DESIGN_SPEC.md)，当前全项目不符合项见 [UI 符合性审计](UI_CONFORMANCE_AUDIT_2026-08-04.md)。

初始走查日期：2026-07-29

当前状态更新：2026-08-04

范围：`OptilandWorkbench.App` 桌面端整体 UI、Dock 工作区和分析结果展示。
说明：P1/P2/P3 段落保留初始走查发现；带日期的更新和“当前实现状态”描述现行行为。分析图默认不在普通 UI 重构中顺手改动，但用户已明确要求的布局、分组、配色和文字修正应按当前契约记录并回归验证。

## 走查方法

- 以主窗口、Ribbon、Dock 工作区、系统属性、镜头编辑器、分析、优化、公差、多配置、材料和查看器面板为主。
- 初始结论来自源码级 UI 结构走查；后续更新结合实现契约、自动化测试和用户提供的界面截图。没有实际验证的视觉结论仍应标记为风险，不能写成已完成。
- 严重度含义：
  - P1：会明显影响主要工作流、误导入口、造成不可预期操作或大面积主题/布局问题。
  - P2：中等可用性问题，常见屏幕或复杂数据下体验会明显变差。
- P3：一致性、可读性、可访问性和打磨问题。

2026-08-24 可靠性与窄窗口复核：图纸页工具条现可换行，设置/预览在 900 宽度以下上下重排；公差编辑器在 800 以下把表格与属性区上下排列，公差向导最小宽度降至 640 并把导航响应式移到上方；库存镜头匹配参数改为可换行字段。四个自绘交互控件提供命名 `AutomationPeer`、键盘平移/缩放/复位和显式焦点边框；2026-08-29进一步发布只读数据摘要及可调用复位动作，不再只有空的 `Custom` 节点。显示设置窗口现可缩放并在短窗口内滚动，图纸缩放图标按钮发布自动化名称。设置 JSON 损坏或超限时会隔离为带时间戳的备份并在状态栏报告，不再静默回退或阻止启动。重置或载入会销毁内容的布局前会单独保护未保存公差数据。以上均为已实现并有定向契约覆盖的行为；Ribbon 的固定高度和全部 Dock 页的更广泛窄宽度复核仍是后续工作。

2026-08-30 主从数据页有限视口修正：设计镜头库、库存镜头查看和材料库不再把含长列表的整个双栏容器放入纵向 `ScrollViewer`。宽布局中主列表与详情获得有限高度并分别滚动；窄布局按视口比例上下重排，列表条目数量不再决定预览画布高度。镜头预览与元数据使用独立星号区域和元数据滚动区，移除了列表、预览和目录表格为适配旧布局设置的固定最小高度。Headless 回归覆盖 925 条镜头在宽窄视口中的选择详情可见性，以及三个页面的有限根行布局。

2026-08-30 分析设置双列间距修正：双列参数网格与底部分隔线、自动应用及命令按钮栏分层布局，按钮栏总宽度不再反向撑大五列参数网格；参数网格按内容左对齐并保持 16 像素列间隔。Headless 回归覆盖设置浮层锚点、内容尺寸和双列网格对齐。

## 全局问题

### P1：Ribbon 信息架构过载，且入口命名不够稳定

Ribbon 当前承载大量分析、设置、工具入口，分析组里还存在重复入口和中英混排名称。例如 `analysis-wavefront` 同时出现在像差分析和波前相关入口中，分析菜单也同时包含 `FFT PSF Cross Section`、`Image Simulation`、`Geometric Image Analysis`、`Fourier MTF vs Field` 等英文/混排名称，见 `src/OptilandWorkbench.App/MainWindow.cs:101`、`src/OptilandWorkbench.App/MainWindow.cs:108`、`src/OptilandWorkbench.App/MainWindow.cs:115`、`src/OptilandWorkbench.App/MainWindow.cs:128`。

Ribbon 视觉上又使用固定高度和固定按钮尺寸：主 Ribbon 高度为 144，按钮宽 78、高 66，按钮内部内容宽 66，见 `src/OptilandWorkbench.App/Shell/MainWindow.Shell.cs:184`、`src/OptilandWorkbench.App/Shell/MainWindow.Shell.cs:274`、`src/OptilandWorkbench.App/Shell/MainWindow.Shell.cs:400`。长标题会依赖换行挤进小块区域，扫描效率和可读性都比较差。

另外主窗口默认选中 Ribbon 第 2 个 tab，见 `src/OptilandWorkbench.App/Shell/MainWindow.Shell.cs:113`。如果第 2 个 tab 是“设置”，用户打开软件后看到的是设置导向，而不是镜头编辑、分析或文件工作流导向。

### P1：全局控件样式过强，可能覆盖局部语义

`App.cs` 对 Button、TextBox、ComboBox、NumericUpDown、DataGrid 选中行等做了全局样式覆盖。尤其是 NumericUpDown 全局关闭 spinner，见 `src/OptilandWorkbench.App/App.cs:124`。光学设计里大量数值参数需要小步进调整，关闭 spinner 会降低精细调参效率，也和“数值输入应提供 stepper/slider/input”的常规工具 UI 预期不一致。

DataGrid 选中行全局设为白字和强调色背景，见 `src/OptilandWorkbench.App/App.cs:147`。这会和优化、公差、玻璃库等表格里的状态色冲突，导致“选中状态”和“业务状态”混在一起。

### P1：主题资源和硬编码颜色混用，暗色/高对比一致性风险大

应用已经定义了浅色/深色主题 token，见 `src/OptilandWorkbench.App/App.cs:220` 和 `src/OptilandWorkbench.App/App.cs:231`。但很多面板仍直接写 RGB 颜色，例如 Ribbon 图标 hover/退出颜色写死为品牌蓝，见 `src/OptilandWorkbench.App/Shell/MainWindow.Shell.cs:412`、`src/OptilandWorkbench.App/Shell/MainWindow.Shell.cs:520`；系统属性 section header 也大量直接按 `IsDarkTheme` 分支设置颜色，见 `src/OptilandWorkbench.App/Panels/SystemPropertiesPanel.cs:319` 到 `src/OptilandWorkbench.App/Panels/SystemPropertiesPanel.cs:330`。

结果是同一控件族在不同面板里会呈现不同的 hover、border、选中和 disabled 状态。后续如果调主题，很容易出现某些面板漏改。

2026-07-31 更新：已将主题调色板扩展为主文字、次文字、弱化、禁用、强调、强调底色文字以及警告/错误/成功状态文字层级；全局 TextBlock、Label、DataGrid 选中行和主要自绘分析/场景控件改为动态资源。原有布局、Ribbon 排列以及波长/视场等分析语义色保持不变。其余面板中仍存在的业务专用硬编码颜色应继续按组件逐步迁移。

2026-07-31 异世界主题：新增独立的 IsekaiTheme.cs，以黑曜石/深皮革底色、旧金强调色、羊皮纸文字层级和奥术蓝场景点缀呈现剑与魔法风格。该主题只替换颜色与绘图资源，不改布局、控件尺寸、Ribbon 排列、现有文案、分析语义色或普通/暗夜主题资源。

2026-08-30 主题包架构：主题选择改由 `ThemeRegistry` 注册完整 `ThemeDefinition`，色板、强调色、实际图标包、Chrome 配置、装饰渲染器和明暗特征不再散落在 `App`、显示设置窗口与 `MainWindow` 的 switch 中；运行时统一经 `ThemeApplicationService` 切换。普通/暗夜主题继续直接使用原始 `StandardLucide` 图标和既有 8/5 圆角、1 像素边框与原 Ribbon 阴影；异世界改用从 Game-icons.net 官方仓库筛选的 `GameIconsFantasy` 填充式 SVG 图标，目录包含 86 个语义映射并覆盖当前界面全部图标用法，同时使用旧金卡片/控件框、状态栏及工作区/视口/对话框双框。图标目录固定上游提交并逐项保存作者/来源，按 CC BY 3.0 署名分发。主题间结构边框厚度保持一致；装饰由零期望尺寸、输入透明的 `ThemeChromeOverlay` 绘制，不改变 Ribbon 命令、布局或命中区域。`System` 只代理实际系统明暗主题，新增主题只注册主题包，不修改业务面板。

2026-09-02 像素风格主题：新增独立 `PixelTheme` 主题包，采用奶油白、天空蓝、亮蓝、珊瑚红、向日葵黄和草绿色的明亮 8-bit 色板。真实整窗与参考图复核后，Ribbon 改为连续浅蓝命令底面，常态命令不再各自套大号卡片框；悬停和按下仍提供硬边反馈。功能区最小高度收至 96，命令最小尺寸收至 48×50，内容宽度收至 44，页签及分组间距同步压缩，使四个常用命令接近经典 8-bit 工具栏的密度；这些尺寸均为像素主题动态资源，普通、暗夜与异世界仍使用原 126 高、78×66 命令尺寸。主题图标改为 CC BY 3.0 的 FatCow Farm-Fresh 3.92 彩色 32×32 PNG，并用最近邻插值保留深色像素轮廓、文件夹黄和软盘蓝等经典桌面语义。Dock 的工作区、工具标题、活动/非活动页签和关闭区使用成套色阶，列表与 DataGrid 统一硬分隔及黄色选择，输入框和下拉框模板获得像素阴影，强调分区修复为蓝底奶油字。中文界面使用 OFL 授权的 Fusion Pixel Font `2026.07.20` 矢量 TTF，并以别名文本渲染保持硬边。卡片、控件框和对话框继续使用零圆角；Ribbon、工作区、视口和对话框装饰使用输入透明的代码矢量层绘制点阵、阶梯角标和像素明暗边。文案、命令、物理光线颜色和分析语义保持不变；Headless 回归验证真实 Fluent 模板、图标目录、字体与文本渲染模式。

2026-08-30 图标语义复核：原先复用 `checkbox-tree`、`magnifying-glass`、`scroll-quill`、`archive-research` 的四组不同操作已经拆分为各自的官方 Game-icons.net 资源。网格/列表树/画中画、搜索/放大/缩小、新建文件/文字输入、包搜索/扫描搜索现在组内来源均唯一；保留方向箭头、返回箭头、圆/椭圆等通过旋转或缩放表达同一图形家族的有意复用。

2026-07-31 主题补全：系统属性折叠卡片、镜头编辑、优化、可制造性、材料库、查看器分隔线和分析报告标题区改用动态主题资源；可制造性错误/警告/通过行新增随主题切换的语义底色，优化操作数的 Zemax 行分类色保持不变。Window、Button、输入控件、列表和 DataGrid 表面不做全局颜色覆盖，继续由 Avalonia Fluent 主题控制。

2026-07-31 明亮主题修复：撤回对 Window、Button、输入控件、列表和 DataGrid 表面的全局颜色覆盖，明亮模式重新使用 Avalonia Fluent 原生控件底色；Light 调色板数值、暗夜/异世界主题、布局和分析颜色均保持原状。

2026-07-31 文案恢复：撤回 1b088115 中未经明确授权的分析显示名称汉化，恢复 FFT、PSF、MTF、RMS、Huygens、Zernike、Jones、Y-Ybar 和 vs. 等既有产品术语。重复入口清理和布局修复保留；后续任何术语翻译或重命名必须有明确的 UI 文案需求。

2026-08-02 设置样式统一：二维/三维视图与全部分析页面通过独立的 `SettingsPanelChrome` 共用设置齿轮、圆角卡片、边框和阴影。明亮主题新增纯白设置卡片表面，避免原 `Surface`（250,250,252）与分析 `SubtleSurface`（242,242,247）造成的灰度不一致；暗夜和异世界主题继续使用各自主题表面色。后续复核发现 Fluent `Button` 的默认灰色背景覆盖了外层白色卡片，因此共享设置按钮现在也直接绑定 `SettingsSurface`；headless 主题测试验证明亮主题下按钮实际背景为 `#FFFFFF`。该变更只调整视觉样式，不修改参数排列、按钮、展开方式、默认值或计算行为。

2026-08-02 窗口布局修复：批量停靠、关闭、浮动、平铺和层叠现在统一剔除不含文档或工具内容的空浮动宿主；会话保存不再写入空宿主，恢复旧会话时也会过滤历史空壳，因此不会再次出现 `Window / No documents open`。Ribbon 将“浮动全部”明确为“全部独立浮动”，说明每个页面各自使用一个原生窗口；“全部停靠/单一 Pane”改为“保留分栏停靠/合并单窗格”，锁定与解锁合并为一个状态切换按钮，“恢复默认布局”也区分为系统初始布局与已保存默认布局。最终交互语义是：只有“全部独立浮动”在软件外创建原生窗口；平铺和层叠会显式把所有页面移回主文档区，切换 Dock 内部 MDI 后分别执行平铺或层叠；合并同样先回收所有页面，再切回单一标签窗格。该变更不复制业务文档，稳定文档 ID 的复用规则保持不变。

2026-08-03 报告入口修正：分析 Ribbon 原“系统报告”按钮改为更短的“报告”下拉按钮，菜单按“表面数据报告、系统数据报告、分类数据报告 / 系统数据摘要、基面数据”排列并保留中间分隔线；基面数据从“光线迹点”迁回报告组。此次并非只替换文案：表面数据报告输出逐面处方表，系统数据报告输出系统/孔径/视场/波长/近轴分类表，分类数据报告按表面角色、材料和几何类型汇总；旧“一阶量、处方报告”名称仅用于兼容既有会话。

2026-08-04 分析结果底部样式统一：普通分析页的“绘图 / 数据 / 文本”页签改用与绘图正文一致的 11.5 字号、30 高度和统一内边距，避免 Fluent 默认页签在高 DPI 下显得过大。分析摘要、视场指标、右侧品牌与文件信息收敛为标题 14、正文 11.5、说明 10.5 的三级字阶，并分别绑定主题主文字、次文字和弱化文字颜色；摘要区高度由 126 缩为 108，右侧品牌/文件分隔固定在 66，状态栏指标列统一为 128 宽及相同字号、颜色和间距。分隔线与表面色继续使用主题资源，因此明亮、暗夜、异世界和后来新增的像素风格主题保持各自配色。

2026-08-04 品牌区资源统一：普通分析页右侧品牌区不再用系统字体拼写 `S.T.A.R. Labs`，改为复用内置 `Assets/Brand/CompanyLogo.png` 横版标识。源图片包含浅色底纹，显示前由统一的 `BrandAssets` 入口去除浅色中性背景并裁切透明边缘，保留原始品牌色和透明通道，以 180×28 边界等比交给 `Image` 直接显示；分析页与工程图标题栏共用同一份底图预处理结果。产品说明、文件名和文件来源统一使用主题次文字色，保留字号层级但不再混用主文字色与弱化文字色。

2026-08-04 分析结果区域二次收敛：矩阵类、单图类分析页面的外层绘图区及承载“绘图 / 数据 / 文本”的结果页签统一绑定 `PlotBackground`，不再因页签或矩阵空白区域透出 `Surface` 而形成灰底套白图，也不再出现同一主题下的底色差异。普通分析摘要与瞳孔像差摘要统一使用 132 的共享页脚高度；普通摘要最多展示四项，其余内容转到“数据”页，避免不同分析内容把页脚撑成不同高度。品牌标识不再通过 Avalonia `OpacityMask` 间接着色，从根本上消除实心矩形和页面间色差。

2026-08-04 分析图响应式布局修正：标准点列图原先把卡片固定为 `280×290`、绘图区固定为 `260×256`，三个视场因此形成 `840×290` 固定画布；矩阵/结构矩阵点列图、离焦点列图以及光线像差/光瞳像差/光程差的成对布局也分别使用固定单元尺寸，并通过 `Viewbox` 把文字和图框一起缩放。以上固定画布和四处整体 `Viewbox` 已全部移除。当前外层网格按视场、波长和结构数量使用星号行列分配可用空间，标题、视场标签、离焦刻度和轴标题保持统一的 11.5 实际字号；仅绘图区放入 `OptionalSquarePlotHost` 保持方形，空间不足时依靠网格重新分配，而不是缩放文字。按钮点击区域、图标尺寸、分隔线厚度等合理的组件级尺寸不属于本次清理范围。

2026-08-04 字体体系收敛：`DisplayTypography` 从单一基础字号扩展为命名字号 token，包括启动标题、页面标题、大标题、窗口标题、空状态标题、分区标题、卡片标题、正文、小正文、紧凑正文、Ribbon 文本、标注和微文案。Ribbon、状态栏、启动页、关于页、材料分析、材料库、系统属性、优化/公差向导、分析参数和分析结果底部区已迁移到这些 token；自绘分析图继续使用图表专用字号和 `DisplayTypography.Scale`。新增 `AppUiFontSizesUseTypographyTokens` 契约测试扫描 App 源码，普通 UI 不允许再出现数字字面量 `FontSize`。

2026-08-04 主题颜色硬编码收敛：IMA/BIM 图像查看器的边框和画布、显示设置窗口的错误/边框/说明文字、分析结果实光线/近轴光线行色均改为主题资源；实光线和近轴光线新增成对的行背景/前景资源并覆盖明亮、暗夜、异世界三套主题。基点、焦平面、主平面、节平面等分析注释色集中到 `AnalysisSemanticColors`，评价函数行色由 `MeritOperandRowPalette` 统一输出明/暗主题的背景和前景，不再由 `OptimizationPanel` 局部写死。新增对比度契约要求分析行色和评价函数业务行色达到 4.5:1；物理波长、光线、制造图纸和算法色标继续作为明确工程语义例外保留。

2026-09-04 主题约束加固：新增 `OrdinaryAppUiColorsUseThemeResources` 架构契约，普通面板、窗口、Shell 和交互控件不得新增裸 `Brushes.*` 或 `Color.FromRgb/Argb` UI 色；主题包自身、制造图纸标准线、物理波长/光线、分析色标和 Zemax 行分类色作为命名工程语义例外。视图方向 `ViewCubeIcon` 与交互画布焦点框改为从主题资源取色，主题切换时同步重绘。

2026-09-04 系统属性一级卡片：六个分区使用完整 `system-property-card` 边框，外侧留白与间距均为 8，标题和外框共用 `SurfaceCard` 圆角，内容由外框裁切；关闭一级卡片阴影，取消原有底部分隔线和标题内缩，常态、悬停、展开保持同一轮廓。保留原有面板宽度、初始展开状态和编辑功能；未修改全局主题定义或其他面板，圆角继续跟随主题（普通/暗夜 8、异世界 3、像素 0）。

本轮定向验证：`SystemPropertiesPanelSectionThemeTests` 使用真实 App 样式，覆盖六个分区、真实鼠标进入/移出、展开/折叠及同一控件的主题切换几何；连同 `ThemeRuntimeTests`、`ThemeResourceTests` 和两个 UI 配色/Chrome 架构契约共 **24/24** 通过。App 默认 Debug 输出构建 **0 警告、0 错误**；未运行全量测试，不据此改写全量测试基线。设置 `OPTILAND_SYSTEM_SECTIONS_CAPTURE_DIR` 并**单独筛选** `SystemPropertiesPanelSectionThemeTests` 可启用 Skia 输出各主题的 collapsed/hover/expanded PNG；不要与采用虚拟绘制后端的其他测试混跑截图，以免共享字体缓存混用。普通主题常态、悬停、展开截图已经人工复核。

渲染复核另发现既有跨主题配色残留：像素切回普通主题后，部分标题仍保留像素配色（`light-after-switch-*` 可复现）。本轮仅修卡片几何，不宣称已修复该全局资源切换问题。

2026-09-04 查看器设置浮层：刷新按钮的常态背景改为与相邻“设置”按钮一致的 `SettingsSurface` 动态资源，不再使用默认按钮灰底；保留现有 hover/pressed 模板及刷新行为，不修改其他按钮或全局主题。`ViewerSettingsButtonThemeTests` 定向 **1/1** 通过，验证普通、暗夜、异世界、像素主题中实际模板背景与设置按钮一致，鼠标进入仍有反馈、移出恢复，且尺寸不变；本次未运行全量测试。

2026-09-04 查看器数字输入与选择区分：本面板的数字框使用 `SettingsSurface` 普通输入底色，下拉框使用 `SubtleSurface` 浅色底。数字框外层仍保持 30 高度，仅对其模板内 `PART_Spinner` 和 `PART_TextBox` 的最小高度作局部约束，避免 Fluent 内层控件过高而裁掉上下边框；不替换数值控件，不更改范围、步长或键入行为，不影响其他面板。`ViewerSettingsInputThemeTests` 覆盖四个数字框、七个下拉框、四种主题、模板边界与键入提交；与刷新按钮及普通 UI 配色契约共 **3/3** 定向通过，未运行全量。单独筛选该输入测试并设置 `OPTILAND_VIEWER_INPUTS_CAPTURE_DIR` 可生成 Skia 渲染图；普通与暗夜图已人工复核。

2026-09-04 查看器紧凑横向表单：标题置于控件左侧，使用面板内部 `ViewerSettingsForm` 统一对齐标签列与输入列；常规宽度按两组并排排列，保留全部 11 个参数（包括线宽）。比例尺/上光瞳、Y 拉伸/下光瞳按参考排列。可用宽度不足 400 时切成单组纵向排列，每项仍保持标题与控件同行；不改动公共 `ResponsiveSettingsGrid` 或其他面板。定向测试补充普通宽度下 6 行参数区高度不超过 210、同行居中和列对齐，以及 360 宽窗口下 11 行不溢出的断言；上述 **3/3** 再次通过，未跑全量，最新普通主题渲染图已复核。

2026-09-04 设置区下拉框背景统一：`SettingsPanelChrome.ApplyInputStyles` 为设置卡片及参数卡片中的 `ComboBox` 提供 `SubtleSurface` 动态背景；系统选项与显示格式窗口也接入该局部样式。分析设置重建时新增的下拉框自动继承，不再只在查看器逐个绑定。样式仅影响设置区下拉框，不修改普通文本/数字输入、区外控件或弹出列表项，原有悬停与选择反馈保留，各主题使用自己的语义背景。`SettingsComboBoxThemeTests` 验证真实模板背景、四主题切换、动态控件、样式幂等、作用域隔离，以及分析/系统/显示三个实际设置界面的接入；与查看器输入/刷新及两项 UI 架构契约共 **6/6** 定向通过，未运行全量测试。以前生成的发布目录不会自动更新此修改，需要重新运行 `Build-Exe.cmd`。

2026-08-04 卡片 chrome 收敛（2026-08-30 主题化复核）：`SettingsPanelChrome` 是普通 UI 卡片/控件框架的公共入口，具体圆角、边框和阴影来自 `ThemeChromeRole` 动态资源，不再由公共类暴露固定数值。普通/暗夜仍为卡片 8、控件 5；异世界可有独立锐角外观，但同角色结构边框厚度一致。材料库列表与详情卡、查看器工具卡、公差操作数编辑卡、显示设置预览框、系统属性编辑卡、Ribbon 命令按钮、Dock 外壳按钮和分析导出按钮已接入公共角色；镜头编辑器移除局部工具栏阴影。`AppUiCardsUseSharedChromeTokens` 阻止新增局部卡片阴影和分裂圆角。图例数据标记、圆形头像裁切、Dock 文档 pill、Splash 装饰和导出版式继续作为明确例外保留。

2026-08-04 长耗时运行状态收敛：新增 `OperationStatusBar` 作为分析、优化和公差这类长耗时操作的统一状态入口。公共控件负责执行中、已同步、结果过期、失败和空闲状态，运行超过 500 ms 后显示不确定进度；可取消任务显示“取消”按钮并调用现有取消令牌，不可取消任务超过 2 秒后显示不可取消说明。`AnalysisPanel`、`OptimizationPanel`、`TolerancingPanel` 已接入该控件，新增 `LongRunningPanelsUseSharedOperationStatusBar` 契约测试，避免后续页面重新散落局部 `ProgressBar` 或只靠文本表达运行中。

2026-08-04 分析设置浮层收敛（2026-08-30 锚点复核）：分析页设置面板不再使用顶部长条 `WrapPanel` 横向铺满窗口，也不再作为 toolbar 内容把绘图区向下压缩。普通分析参数、Zemax 风格参数和查看器设置统一为左上浮层卡片；分析卡片与工具栏“设置”按钮左边缘对齐并紧接工具栏下方，覆盖在图形结果区上方。背景使用 `SettingsOverlaySurface` 半透明主题资源，文字和输入控件保持正常不透明；参数按两列字段排列，列宽由标签和输入控件内容测量，不再以 `Stretch + MaxWidth` 居中或制造空白；底部保留自动应用、应用、确定/取消、保存/载入/重置等操作区。`AnalysisSettingsOverlayAnchorsBelowToolbarAndSizesToContent` 以 1400px 结果区实测卡片锚点和非铺满宽度。

### P1：关键设置经常默认隐藏，但操作会自动触发

分析面板的设置 host 默认隐藏，见 `src/OptilandWorkbench.App/Panels/AnalysisPanel.cs:112`；镜头编辑器的“表面属性与组件”也默认隐藏，见 `src/OptilandWorkbench.App/Panels/LensEditorPanel.cs:137`。这降低了关键参数的可发现性。

同时分析设置里存在“自动应用”行为，设置关闭时也可能刷新分析，见 `src/OptilandWorkbench.App/Panels/Analysis/AnalysisPanel.Parameters.cs:405` 到 `src/OptilandWorkbench.App/Panels/Analysis/AnalysisPanel.Parameters.cs:423`。对耗时分析来说，用户调整下拉框或数值框时自动重算，容易造成卡顿和“还没改完就开始跑”的体验。

### P2：窗口和面板大量使用固定尺寸，响应式能力不足

历史实现曾给主窗口、分析设置和材料库施加较大的固定宽度；当前主窗口下限已降为 640px，分析设置宽度由内容决定，材料库主从区域改由响应式容器管理。仍需在新增 Dock 页面时防止重新引入大 `MinWidth` 或固定文档画布尺寸。

这些固定尺寸在大屏上可接受，但当用户把面板停靠到窄列、使用小屏或缩放比例较高时，容易出现横向滚动、挤压、按钮换行失控。

### P2：Dock 工作区默认结构偏“工程师可用”，但导航模型不够显性

默认 Dock 布局左侧只有系统选项，中央是镜头编辑器文档，见 `src/OptilandWorkbench.App/Services/WorkspaceDockFactory.cs:90` 到 `src/OptilandWorkbench.App/Services/WorkspaceDockFactory.cs:120`。分析、优化、公差等作为文档打开，这种模型灵活，但高度依赖 Ribbon 入口；如果用户错过 Ribbon，缺少一个稳定的工作流导航或最近/常用面板入口。

会话布局读取直接反序列化 JSON，见 `src/OptilandWorkbench.App/Services/WorkspaceSessionStore.cs:237`、`src/OptilandWorkbench.App/Services/WorkspaceSessionStore.cs:249`、`src/OptilandWorkbench.App/Services/WorkspaceSessionStore.cs:316`。从 UI 角度看，异常或过大的布局文件可能导致启动期恢复失败、卡顿或回退体验不稳定。

## 面板级问题

### P1：公差面板打开即创建默认操作数

`TolerancingPanel` 构造完成后直接调用 `AddOperand()`，见 `src/OptilandWorkbench.App/Panels/TolerancingPanel.cs:144`。这意味着用户只是打开面板，就会看到或生成一个默认公差操作数。对工程软件来说，打开面板不应暗含编辑动作，否则保存、撤销和用户心理模型都会变得不清晰。

### P2：镜头编辑器密度高，但高级组件入口低可见

镜头表格使用大量固定列宽，例如多个列通过 `DataGridLength(width)`、112、96、106、240 等固定尺寸构建，见 `src/OptilandWorkbench.App/Panels/LensEditorPanel.cs:341`、`src/OptilandWorkbench.App/Panels/LensEditorPanel.cs:370`、`src/OptilandWorkbench.App/Panels/LensEditorPanel.cs:499`。密集表格符合光学设计软件习惯，但目前缺少明显的列管理、常用列/高级列分层或横向滚动提示。

组件编辑器使用 `WrapPanel`，见 `src/OptilandWorkbench.App/Panels/LensEditorPanel.cs:77`，并由 240px 宽按钮打开，见 `src/OptilandWorkbench.App/Panels/LensEditorPanel.cs:125`。打开后几何、孔径、光栅、薄透镜等参数容易混排，且不同表面类型下不相关字段也容易形成视觉噪音。

### P2：系统属性把基础光学数据折叠得过深

系统属性默认显示多个折叠 section，其中“波长”“环境”“高级”都是后置区块，见 `src/OptilandWorkbench.App/Panels/SystemPropertiesPanel.cs:80` 到 `src/OptilandWorkbench.App/Panels/SystemPropertiesPanel.cs:82`。波长对绝大多数分析是基础输入，折叠过深会让新用户误以为只需要孔径和视场。

环境区内还有“当前仅保存环境参数，暂不启用温度补偿计算”的说明文字，见 `src/OptilandWorkbench.App/Panels/SystemPropertiesPanel.cs:563`。这类状态很重要，但现在放在普通说明文本中，容易被忽略，也容易让用户误会环境设置已经参与全部计算。

已于 2026-07-29 按明确需求在系统选项中加入“材料库”折叠区。界面使用“当前玻璃库 / 可用玻璃库”双列表以及加入、移出、优先级上移和优先级下移操作；当前列表顺序参与未限定厂商玻璃名的解析，并进入撤销、STAROPT 快照及 ZMX `GCAT` 往返。至少保留一个当前目录，避免空选择产生隐式回退。

2026-09-04 更新：系统属性里的玻璃库双列表改为局部卡片式条目模板，去掉原生 `ListBox` 表格分隔线和外框，列表项的圆角、边框、悬停与选中状态统一使用主题资源，因此与同面板的视场/波长卡片保持一致。该变更只调整“当前玻璃库 / 可用玻璃库”的显示样式和说明文字层级，不改变目录加入、移出、排序、撤销、ZMX `GCAT` 往返或材料解析逻辑；现有契约测试同时覆盖目录转移行为和局部条目模板存在性。

### P2：分析设置布局窄屏表现弱，且结果导航存在例外

分析面板默认参数区是 `WrapPanel`，见 `src/OptilandWorkbench.App/Panels/AnalysisPanel.cs:28`；部分 Zemax 风格设置页则是固定列宽 Grid，见 `src/OptilandWorkbench.App/Panels/Analysis/AnalysisPanel.Parameters.cs:361`、`src/OptilandWorkbench.App/Panels/Analysis/AnalysisPanel.Parameters.cs:376`。两套布局在不同分析之间切换时，用户会感到设置项位置和阅读顺序不稳定。

结果页大多数使用“绘图 / 数据 / 文本”三 tab，见 `src/OptilandWorkbench.App/Panels/Analysis/AnalysisPanel.Results.cs:87` 到 `src/OptilandWorkbench.App/Panels/Analysis/AnalysisPanel.Results.cs:94`。这是合理方向，但特殊报告类视图会走不同构建分支，可能造成有些分析不像同一个产品体系。

分析图的坐标含义、曲线分组、单位、图例和矩阵维度属于产品契约。未获得明确需求时仍保持冻结；2026-08-03 已按明确反馈完成的展示修正如下。

#### 当前分析结果 UI 契约

| 分析 | 当前布局与文字规则 |
| --- | --- |
| 全部分析图 | 图内标题、刻度、坐标轴、图例、色标、矩阵标签、fan 卡片标题和空图提示统一使用同一分析图字号；标题或重点信息只能通过字重区分，不再使用忽大忽小的字号层级。 |
| 标准点列图 | 按视场分组，自动规划平衡网格；点列坐标是像面 X/Y 截距。 |
| 离焦点列图 | 视场为行、五个离焦位置为列；每个小图为 `120×120` 方形，隐藏重复轴标题和刻度文字，只保留统一视场标签、离焦列值和离焦说明。不得把 X/Y 两轴都显示成“像高”。 |
| 全视场点列图 | 单图外层默认方形，X/Y 数据等比例；图例位于独立区域，不参与方形尺寸计算。按波长着色时按波长分组，按视场着色时真正按视场拆分系列。 |
| 普通矩阵点列图 | 视场为行、波长为列；小图为 `140×140` 方形，隐藏重复轴标题，以最小绘图边距扩大点列区域。 |
| 结构矩阵点列图 | 视场为行、结构为列；每个“视场 × 结构”单元叠加全部所选波长，蓝/绿/红波长图例可分别开关。不得把三个波长继续拆成三个格子。 |
| 光迹图 | 单图外层默认方形；按视场或波长着色时，图例标签、颜色和过滤分组保持一致。 |
| 干涉图 | 作为二维瞳面空间分布，单图外层默认正方形，X/Y 数据等比例；不得随结果区宽高比拉伸成矩形。 |
| 色焦移 | 保持普通曲线图的自适应宽屏布局；横轴为焦移、纵轴为波长，不套用二维空间图的方形视口。 |
| 光线像差图 | 每个视场的 `P_y` 与 `P_x` 紧邻组成组合卡片；多个视场形成平衡网格，末行居中，组合卡片内子图保持接近正方形。 |
| 瞳孔像差图 | 与光线像差图使用相同的视场配对布局；所有视场、波长和 X/Y 子图共用全局对称纵轴 `±scale`，说明区只保留日期、最大缩放、波长和像面。 |
| 其它多子图 | 使用各自的自动铺满或固定矩阵契约；结果区不提供手动“方形子图”开关。 |

波长色由真实波长值选择稳定的工程约定色，不按系列序号循环。显示采用高饱和、高辨识度风格：Fraunhofer F/d/C 线 `486.1 nm`、`587.6 nm`、`656.3 nm` 分别使用亮蓝 `#008CFF`、亮绿 `#00C853`、鲜红 `#FF3430`。d 线绿色是工程曲线约定，真实 `587.6 nm` 单色光外观偏黄，二者不得混为一谈。不可见的紫外/红外使用蓝灰中性色；视场、结构、频率等离散类别仍使用主题分类色；渐晕红是状态色，不属于波长映射。

上述布局主要由 `AnalysisPresentationKind`、`AnalysisPlotOptions.DefaultSquareViewport`、`AnalysisPanel.Plots.cs` 和 `AnalysisPlotControl` 实现。轴标题即使清空，也必须同步清除不再适用的 `AnalysisAxisQuantity/AnalysisAxisUnit`，否则格式化器会重新生成轴文字并挤占小图空间。

2026-08-04 更新：独立“畸变”分析入口已移除；场曲和畸变在同一个组合页面中并列展示。旧 `Distortion`/“畸变”名称只用于会话迁移并自动转到“场曲/畸变”，不再创建第二套页面。干涉图默认使用方形外层视口并保持二维数据等比例；色焦移保持普通曲线图的自适应布局。

### P2：优化面板状态色和编辑行为过重

已于 2026-07-29 修正评价函数表的单一状态色：编辑器现在按 Zemax 操作数类型应用稳定的浅色行色，参考系统中出现的 `TTHI`、`OPLT`、`EFFL`、`PMAG`、`CONS`、`DIVI`、`REAR`、`PETZ`、`MNCA`、`MNEA`、`MNCG`、`MNEG`、`MXCG`、`MXEG` 和 `DMFS` 均有对应色；`BLNK` 保持白色，同族操作数使用一致色系，未知类型使用中性回退色。错误状态优先覆盖为红色。

2026-08-04 修正 ZMX 评价函数漏行：参考镜头的 103 行评价函数现在按 Zemax 原顺序导入，不再只显示约 29 行。2026-09-04 进一步把常见操作数束扩展到 124 个定义级可执行代码，覆盖 `TRAR`、`TTHI/TGTH`、`REAR/RANG`、基础数学与行约束（含 `DIVB/PROB/OSUM/QSUM/EQUA` 定义级语义）、常见厚度/边厚/曲率/圆锥/半口径、`WLEN/INDX`、`MNIN/MXIN/MNAB/MXAB`、`POWR`、若干一阶量以及 `CTGT`、`PMAG`、`PETZ`、`MXEG` 和 `GOTO/ENDX/OOFF/SKIN/SKIS/USYM`；注册表按 2026 R1 实测扩展到 383 个顺序兼容代码，`DIMX` 等语义不完整项按八个 Zemax 参数槽位显示为只读记录。这里的目标是先保证“看见的行、顺序和源参数一致”，并逐步把可验证语义接入；只读行不参与 Workbench 评价函数数值，新增执行路径在 Zemax golden 对照前也不能据此外观宣称与 Zemax 优化器等价。

选中行不再使用全局强调色覆盖整行，而是保留类型底色、恢复深色文字并使用强调色边框，因此颜色语义在键盘或鼠标选择后仍然可见。实现见 `src/OptilandWorkbench.App/Panels/MeritOperandRowPalette.cs` 和 `src/OptilandWorkbench.App/Panels/OptimizationPanel.cs`。

尚未实现的边界是 ZOS-API `IMFERow.RowColor` 的 `Color1`–`Color16` 自定义覆盖、逐行“无颜色”以及全局 `Color Rows` 偏好开关的持久化；这些能力不得仅凭当前默认色外观标记为完成。

工具栏用两个 `WrapPanel` 承载新增、删除、上移、下移、向导、刷新、运行等动作，见 `src/OptilandWorkbench.App/Panels/OptimizationPanel.cs:80` 和 `src/OptilandWorkbench.App/Panels/OptimizationPanel.cs:94`。动作多但层级不明显，用户不容易区分“编辑评价函数”和“运行优化”的主次。

2026-08-27 已纠正优化入口的名称失实：Ribbon 只显示实际实现的阻尼最小二乘与贪心随机扰动搜索，不再把随机坐标扰动称为差分进化、盆地跳跃或“锤形优化”。玻璃替换模板当前明确为玻璃目录与评价函数的人工替换工作流，不表示已经实现 Zemax Glass Expert 等价算法。

### P2：多配置面板过于简化，容易误导功能边界

多配置面板工具栏只暴露新增、激活、选择表面和应用厚度，见 `src/OptilandWorkbench.App/Panels/MultiConfigurationPanel.cs:50` 到 `src/OptilandWorkbench.App/Panels/MultiConfigurationPanel.cs:62`。表格列也主要是名称、表面数、总长、有效焦距等摘要，见 `src/OptilandWorkbench.App/Panels/MultiConfigurationPanel.cs:101` 到 `src/OptilandWorkbench.App/Panels/MultiConfigurationPanel.cs:106`。

这会让用户以为多配置只能编辑厚度，而不是一个可管理多变量/多操作数的系统级能力。入口本身没有错，但表达的能力边界偏窄。

### P2：查看器设置浮层可能遮挡主场景

3D/2D 查看器把设置区作为覆盖层隐藏/显示，见 `src/OptilandWorkbench.App/Panels/ViewerPanel.cs:227`。浮层适合快速调参，但在小屏、复杂光路或放大查看局部时会遮挡视图。分隔线等局部颜色也有硬编码，见 `src/OptilandWorkbench.App/Panels/ViewerPanel.cs:318`。

查看器设置里同时包含渲染模式、光线范围、颜色、比例尺、口径、帧和箭头等选项。它们分别属于“追迹输入”和“显示偏好”，混在一起会增加理解成本。

### P3：材料/制造相关面板宽度和说明文本偏硬

材料库和材料分析面板有较大的最小宽度与固定控件宽度，见 `src/OptilandWorkbench.App/Panels/MaterialDatabasePanels.cs:102`、`src/OptilandWorkbench.App/Panels/MaterialAnalysisPanel.cs:33`。在 Dock 文档区域变窄时，容易强制横向空间。

材料分析中存在显式操作说明文字，例如滚轮缩放等提示，见 `src/OptilandWorkbench.App/Panels/MaterialAnalysisPanel.cs:108` 附近。少量提示可以接受，但如果大量出现，会让专业工具界面显得像教程页，并占用数据区域。

### P3：若干小控件语义不够自解释

镜头编辑器中无限值相关控件使用“∞”作为 checkbox 内容，且 NumericUpDown 局部也关闭 spinner，见 `src/OptilandWorkbench.App/Panels/LensEditorPanel.cs:602`。如果没有 tooltip 或相邻解释，用户不一定能立刻判断它控制的是“无限焦距”“无限周期”还是“禁用数值”。

## 建议优先级

1. 先处理 P1：Ribbon 信息架构、关键入口命名、全局控件样式、主题硬编码、公差面板打开即编辑、分析自动应用的可控性。
2. 再处理响应式基础：减少固定宽度/高度，给窄 Dock 和高 DPI 场景做布局预算。
3. 然后处理核心工作流面板：镜头编辑器列管理、系统属性基础区块、优化/公差工具栏主次、多配置能力表达。
4. 最后做一致性打磨：tooltip、状态文本层级、暗色主题、空状态、错误状态和辅助说明。

## 分析图冻结说明

2026-09-04 干涉图左下角按用户参考图改为专用紧凑摘要：日期、分析波长和实际视场、峰谷值（波长）、分析面、科学计数法出瞳直径以及条纹/倾斜槽位。新增 `AnalysisPresentationKind.Interferogram` 和类型化 `InterferogramSummaryDto`，从原始分析结果传递未舍入数据；视场由归一化 Hx/Hy 恢复为真实定义坐标并显式携带角度/毫米单位，不解析显示标题或本地化参数行。X 非零时同时显示 X/Y，缺失数据用 `—`；不固定为参考截图的轴上视场或数值。当前“干涉图”仍由 `WavefrontAnalysis` 生成 OPD 热图，没有独立干涉条纹倍率与参考波 X/Y 倾斜参数，这三项显示 `—`，悬停摘要可查看说明，不能将显示缩放、瞳孔偏移或移除倾斜选项冒充这些量。此任务不修改计算、色图、曲线或完整数据/文本；右侧产品与文件区域维持共享尺寸、字体、颜色和 Logo。窄窗口仅在左侧行末省略，不挤压右侧。Application 构建及 App `Compile` 检查通过，类型化数据传递定向测试 **2/2** 通过（角度与物高视场）；另增加 600/1200 DIP 布局、缺失数据、分析类型隔离和右侧区域一致性用例，尚待用户保存关闭占用程序后执行默认构建及渲染复核。未运行全量测试或打包程序；数据传递测试不构成 Zemax 光学数值对标通过。

2026-09-04 光程差图（Optical Path Difference）左下角按用户参考图精简：保留既有标题，下面只显示日期、最大缩放比例（Waves）、带同色下划线的三位小数波长标签和分析面，不再列出通用参数摘要或“其余项见数据页”。与 Ray Fan 共用局部摘要控件，按 `AnalysisPresentationKind` 分派；比例取真实子图纵轴界限及类型化单位，不固定为截图的 5.000，也不从轴标题猜测单位。右侧品牌/文件信息继续使用共享区域，字体、颜色、Logo 和尺寸保持不变；曲线、交互图例、数据和文本报告不变。新增光程差入口、0.750/2.500 Waves 单位与选面、标题不参与分派的定向契约，并将光程差加入共享右侧信息区的跨主题/尺寸检查。App 的 `Compile` 目标和测试项目独立编译通过，`git diff --check` 通过；应用仍有未保存内容并占用默认输出，以上新用例尚未执行、实际渲染待完成，不能记为已通过的运行验证。未运行全量测试或打包程序。

2026-09-04 分析设置底部“应用 / 确定 / 取消 / 保存 / 载入 / 重置”统一使用局部按钮工厂：文字水平、垂直居中，最小宽度由 86 缩至 64 DIP，左右外边距由 3 缩至 2 DIP，内边距为 10/3 DIP。常规双列设置和图像模拟设置均使用此规则，保留 `MinWidth` 而非固定宽度以适应大字体；不修改按钮颜色、点击逻辑、全局主题或其他工具栏。新增 `AnalysisSettingsFooterLayoutTests` 两个定向用例，检查两个设置入口的六个按钮、实际文字中心、默认紧凑宽度和大字体扩展。默认输出定向测试尝试因正在运行且有未保存标记的应用占用 Core/Application DLL 而在构建阶段失败，测试尚未执行；需用户保存关闭后完成默认构建和渲染复核，不能记为通过。未运行全量测试或打包程序。

2026-09-04 赛德尔系数报告补齐波长、横向、轴向逐面系数表以及波前汇总，沿用等宽可选择文本和双向滚动，不修改全局主题或分析图布局。定向验证为 10 项通过、1 项因 `123456` 已发现的全系统数值差异明确跳过；独立 Skia 显示复核通过。默认桌面重建仍被运行中的应用占用阻塞，未运行打包。详细数值定义、对标范围和待解决项见 [赛德尔系数报告](SEIDEL_COEFFICIENT_REPORT.md)。

2026-09-04 统一分析页脚右侧信息区：场曲/畸变不再使用独立的 `3*:2*:1*` 布局和 `Viewbox` 缩放，而是复用普通分析已有的 `2*:1*` 页脚。场曲、畸变两个摘要仅在左侧三分之二区域内等分排布；右侧保持完整的 132 DIP 内容高度、64 DIP 品牌行及相同文件信息行。在同一主题和窗口宽度下，各分析的右侧区域尺寸、字体/字号/字重、文字颜色、Logo 资源和显示尺寸完全一致，标题栏不再占用右侧高度。窄窗口中的左侧摘要使用省略号，避免跨列挤占信息区；不改动其他主题资源、图形数据或原有无品牌区的瞳孔像差页脚。相关页脚定向测试 **16/16** 通过，覆盖 600/1200/2048 DIP 宽度、四种主题和五类分析；另以 Skia 实际绘制验证 **7/7** 通过，人工复核 `artifacts/validation/field-curvature-footer-unified/field-curvature-footer.png` 与 `footer-Standard.png` 中右侧完全一致、无裁切。两组筛选有重叠，不累计为全量基线；默认 Debug 输出已更新，未运行全量测试或打包程序。

2026-09-04 场曲/畸变组合页脚按用户参考图改为双摘要：上方分别显示“场曲”和畸变标题，左侧保留日期、最大视场、弧矢/子午场曲及“图例对应于波长”，右侧保留日期、最大视场和最大畸变，产品与文件信息位于最右侧。移除通用“最大像面偏移 / 畸变模型”列表；只有实际模型为 `f-tan`、`f-theta` 或 `smia-tv` 时才在标题中带对应模型名，有限共轭的线性高度模型不冒充 F-Tan。最大视场使用所显示子图的视场坐标范围与类型化单位，避免真实像高转换后把内部角度元数据错误显示成毫米；畸变按子图 X 轴单位分别显示百分比或绝对长度。弧矢/子午摘要复用现有计算指标、保留符号，不改变其计算定义，也不复制 Zemax 截图数值。此修改仅作用于 `FieldCurvatureAndDistortion` 展示类型，曲线、坐标、可切换图例、数据/文本和单独场曲分析保持不变。相关页脚与组合分析单位契约 **11/11** 定向通过；独立 Skia 实际镜头页脚渲染 **1/1** 通过并人工复核 `artifacts/validation/field-curvature-footer/field-curvature-footer.png` 无裁切，未运行全量测试，不构成新增光学数值一致性结论。

2026-09-04 光线像差图（Ray Fan）左下角按用户提供的 Zemax 截图精简：保留既有分析标题，标题下只显示日期、最大缩放比例、带同色下划线的三位小数波长标签和分析面；不再显示采样点数、视场/波长数量或“其余项见数据页”提示。比例根据各子图真实纵轴边界和 `AnalysisAxisUnit` 换算，长度显示 µm、无焦角度保持实际角度单位，不固定成截图中的 5.000 µm；分析面跟随结果，不把所选中间面误写成像面。完整数据/文本、右侧产品/文件信息、曲线、原有可切换图例及其他分析页脚保持不变。`RayFanFooterTests`、Ray Fan 设置契约、入射角页脚隔离及共享高度共 **7/7** 定向通过；另以独立 Skia 进程复跑真实页脚测试 **1/1**，人工复核 `artifacts/validation/ray-fan-footer/ray-fan-footer.png` 无裁切，未运行全量测试。这是页脚呈现验证，不是光学数值与 Zemax 的新增一致性结论。

2026-09-04 入射角图页脚按用户要求精简：`Angle vs Image Height` 及其扫描瞳孔/扫描视场入口使用独立的 `AngleVsImageHeight` 展示类型，绘图页左下角仅保留分析标题和生成日期时间，不再列出参数数据或“其余项见数据页”提示。右侧产品/文件信息、曲线、图例、坐标和完整数据/文本输出保持不变，其他分析页脚不受影响。入射角相关契约及共享页脚高度共 **5/5** 定向测试通过，未运行全量测试；新增回归验证标题改名不改变展示分派，普通分析仍显示摘要。

分析图属于与 Zemax 一致性核对的一部分，不应在普通 UI 改版中顺手调整。2026-08-03 已明确要求并完成的点列图、fan 图、瞳孔像差和波长色修正，以“当前分析结果 UI 契约”为准。后续涉及 `AnalysisPanel.Plots.cs`、`AnalysisPlotControl`、波前/PSF/MTF/圈入能量等图形控件时，应先确认：

- 是否改变了图的坐标含义、曲线分组、单位、图例、色标或矩阵排布。
- 是否会影响与 Zemax 截图/结果对比。
- 用户是否明确授权修改图形表现形式，以及文档中的当前契约是否需要同步更新。

如果只是修复入口、设置项、主题 token、布局容器或面板导航，也应避免改变分析图的视觉输出。

## 2026-09-05 曲率半径单元格求解

移除独立“R 变量”列，在半径值右侧增加 24 DIP 标记区，固定为空白、变量为 `V`、拾取为 `P`。点击打开“固定 / 变量 / 拾取”弹窗，固定状态打开时默认选择变量，确认才提交；拾取提供前序源面和曲率比例，数值框无增减箭头。保持“T 变量”入口不变，不扩充为其他 Zemax 求解类型。

拾取已连接实际半径更新、优化、撤销/重做和原生保存；旧版不能无损映射的拾取参数保留并明确提示。默认 App 构建 0 警告、0 错误，组合定向回归 **43/43** 通过；独立 Skia 界面复跑 **3/3** 通过并复核变量/拾取弹窗截图，重复用例不累加。未运行全量测试或打包。完整定义、配置行为及验证边界见[曲率半径求解入口](RADIUS_SOLVE_EDITOR.md)。

## 2026-09-05 普通主题操作按钮蓝色状态

“添加视场”、添加/删除、应用/确定/保存等使用默认填充的普通操作按钮统一为淡蓝色 `#D9ECFF`；悬停为 `#B9D9FF`，按下为 `#91C3FF`，松开并移出后恢复默认色。禁用状态沿用原主题表现，不显示可点击的蓝色反馈。只改背景，不改文字、图标、圆角、尺寸或点击逻辑。

通过 `StandardActionButtonStyles` 精确匹配实际 Light 主题，不向全局资源字典写入按钮颜色；暗夜、异世界、像素主题保持原配色（像素主题虽继承 Light，但不会匹配）。跟随系统且实际为 Light 时使用相同规则。侧栏标题、Ribbon 命令和 accent 按钮不覆盖其专用样式；设置/刷新及透明工具按钮的显式默认背景继续保留。

默认 App 构建为 **0 警告、0 错误**。`StandardActionButtonThemeTests` 5 项和相邻设置/侧栏/像素按钮 3 项组合检查 **8/8** 通过，覆盖真实鼠标悬停/按下/松开、添加视场只执行一次、禁用、新建弹窗按钮和三个其他主题的往返切换。独立 Skia 再跑添加视场用例 **1/1** 通过，并人工核对 `artifacts/validation/action-button-blue/button-normal.png`、`button-hover.png`、`button-pressed.png`；这是重复的渲染验证，不累加测试基线。未运行全量测试或打包程序。

## 2026-09-05 文档标签圆角

“镜头数据”及同一文档栏中的标签在普通/暗夜模式下使用 6 DIP 四角圆角，覆盖未选中、选中、活动和悬停状态。只覆盖 Dock 的 `DockDocumentTabItemCornerRadius` 资源，沿用原标签模板，不改变高度、内外边距、字体、颜色、关闭按钮、拖拽和页面切换行为。异世界和像素主题显式保留原有 0 圆角，避免从普通主题切换过去时继承新的圆角值；不影响 Ribbon 或分析结果页内部标签。

已核对当前依赖版本 `Dock.Avalonia.Themes.Fluent 12.0.0.2` 的[上游标签模板](https://github.com/wieslawsoltes/Dock/blob/ebbf3d46a9c9e7b4d6b898df2a685a2f956344ee/src/Dock.Avalonia.Themes.Fluent/Controls/DocumentTabStripItem.axaml)：`PART_TabBody` 的背景、边框和圆角绑定同一个标签控件，因此无需复制或替换模板。

`DocumentTabCornerTests` 三项定向回归已通过，覆盖主题资源、真实标签模板各状态、切换主题后的圆角和同一主题下仅改变圆角时尺寸不变。尺寸检查固定字体条件，不要求本来字号不同的主题宽度相等。默认 App 输出已重新构建（0 警告、0 错误）。可通过 `OPTILAND_DOCUMENT_TAB_CAPTURE_DIR` 输出独立 Skia 诊断图；本轮通过结论为控件契约检查，不代表所有跨主题外观均已验收。未运行全量测试或打包程序。

## 2026-09-05 光瞳切趾选项精简

系统孔径中的“光瞳切趾”下拉菜单仅提供均匀、高斯、余弦立方三项，显示名称不带“（Zemax）”后缀。内部继续使用原有 Zemax 类型键，因子编辑、GFAC 导入及保存语义不变；尤其不把旧版高斯的 σ 当作 Zemax 高斯因子。

无切趾和旧版均匀模型显示为“均匀”。旧工程中的其余模型不加入可选列表，显示“旧版设置（保留）”，悬停可查看原类型；其参数不再提供编辑。修改孔径值等其他设置时保留原类型和参数，只有用户明确选择三种新类型之一时才替换。Core 模型及旧文件序列化保留兼容性，不删除已有工程数据，也不新增切趾类型。

切趾导入、因子、保存与选项契约定向测试 **25/25** 通过；`ApodizationPickerTests` 的 **11/11** 个界面用例现已通过，覆盖三种显示名称/因子编辑、八种旧设置的保留和显式切换。默认 App 输出已重新构建（0 警告、0 错误），界面结论来自实际 Headless 控件交互，不只是编译检查。未运行全量测试或打包程序。

## 2026-09-05 全局数值输入不显示增减箭头

按用户要求，在 App 级 `NumericUpDown` 样式统一设置 `ShowButtonSpinner=false`，覆盖系统孔径值、各分析设置、普通面板、独立弹窗和运行时新增的数值控件，以及全部主题。仓库扫描未发现启用箭头的局部覆盖。保留数值控件本身及其文本编辑、格式、最小/最大值、绑定和校验逻辑；不改变键盘/滚轮行为，不影响普通文本框或下拉菜单的展开箭头。

`NumericInputChromeTests` **2/2** 项定向界面回归现已通过，检查所有主题的新建数值框、内部按钮隐藏及空间回收、直接输入、系统孔径值和显示设置弹窗。默认 App 输出已重新构建（0 警告、0 错误）；测试实际创建控件并检查布局。未运行全量测试或打包程序。

## 2026-09-05 表面类型与属性分类面板

2026-09-05 镜头编辑器提供类型、绘图、孔径、散射、倾斜/偏心、物理光学、膜层、导入、复合、偏振十个分类。已接入原有组件、光阑、膜层、固定净半径的事务编辑；外观、全局参考面等缺少底层语义的选项明确禁用，不宣称完整 Zemax 等价支持。初版 11 项服务/组件及 3 项界面回归通过；后续标题简化后的界面验证以下文为准。完整字段边界和验证范围见 [表面类型与属性面板](SURFACE_PROPERTIES_EDITOR.md)。

同日按用户新截图将宽大标题按钮简化为“展开/收起 + 表面 N 属性 + 上一面/下一面”紧凑横条，三个圆形按钮直径 28 DIP。标题跟随当前表面，左右按钮同步表格选中行与属性内容并在首尾禁用；默认收起，展开分类保持不变。导航先提交表格数值，属性草稿不自动应用，修订变化后旧导航取消。半径单元格的求解入口保持不变，不恢复旧“R 变量”列。默认 App 构建 0 警告、0 错误；属性面板 6 项、半径求解 3 项和右键操作 5 项组合 **14/14** 通过。独立 Skia 属性面板 **6/6** 复跑通过，复核紧凑标题截图，重复用例不累加；未运行全量测试或打包。

## 2026-09-05 镜头数据右键插入与删除

移除镜头数据表上方的“添加 / 删除”按钮，右键所在行即成为操作对象，菜单仅包含“下插入、上插入、删除”，包括在数值单元格中右键。未加入参考截图中的复制、粘贴、隐藏等其他命令；表头和空白处不打开表面菜单。

物面只能下插入、像面只能上插入，两者均不可删除。插入或删除是单次可撤销事务；插入后选中新行，删除后选中邻近行。保持原有新面默认值（半径 40、厚度 5、空气），不宣称复刻 Zemax 新面默认值。已有丰富几何/孔径/膜层保留，拾取关系与多配置覆盖的面序号同步；未新增评价函数或公差操作数的面序号迁移机制。

打开菜单前提交正在编辑的单元格，未改变的数字不重复提交；菜单打开后若工作区修订变化，旧菜单命令失效，避免对已重排的面序号误操作。不改变主题、追迹算法或分析图。

默认 App 构建 **0 警告、0 错误**；本次插入/删除与相邻回归 **21/21** 通过，另补跑上述切趾、数值框、属性面板和标签的 **19/19** 界面用例，组合筛选共 **40/40**。独立 Skia 进程再次运行右键 **5/5** 用例，并人工核对 `artifacts/validation/lens-surface-context-menu/lens-surface-menu.png`：无旧按钮、菜单仅三项且选中右键行。重复筛选不累加为全量基线，未运行全量测试或打包程序。

## 2026-07-31 异世界 Ribbon 剑形装饰

异世界主题最初使用独立 `IsekaiRibbonChrome` 矢量层；2026-08-30 已迁移为通用 `ThemeChromeOverlay` 和注册在主题包中的 `IsekaiThemeDecorationRenderer`。现有深色皮革/黑钢底纹、旧金框线、铆钉、剑柄、蓝色宝石与贯穿式剑刃保持不变，并扩展工作区双框和符文角标。通用渲染层根据当前主题包选择装饰器，输入透明且不请求布局空间，因此明亮、暗夜及未来主题不会受到异世界实现污染。

本次不修改 Ribbon 页签和命令的名称、语言、顺序、尺寸、分组或点击逻辑；现有 `TabControl` 仅与装饰层叠放，布局仍由原控件完成。装饰采用代码矢量绘制而非固定分辨率位图，以避免高 DPI 和窗口宽度变化时的拉伸失真。
