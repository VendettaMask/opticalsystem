# 赛德尔系数报告（2026-09-04）

## 已实现

`SeidelCoefficientsAnalysis` 不再只输出一张七项系数表。完整文本报告按以下顺序输出，每张逐面表均包含光阑、像面和累计行：

| 表格 | 列 |
| --- | --- |
| 赛德尔像差系数 | SPHA/S1、COMA/S2、ASTI/S3、FCUR/S4、DIST/S5、CLA/CL、CTR/CT |
| 赛德尔像差系数（波长） | W040、W131、W222、W220P、W311、W020、W111 |
| 横向像差系数 | TSPH、TSCO、TTCO、TAST、TPFC、TSFC、TTFC、TDIS、TAXC、TLAC |
| 轴向像差系数 | LSPH、LSCO、LTCO、LAST、LPFC、LSFC、LTFC、LDIS、LAXC、LLAC |

末尾补充系统波前系数汇总，以及 W220S、W220M、W220T 场曲波前系数。波长、物/像空间主光线和边缘光线斜率、佩兹伐半径、光学不变量继续保留。

桌面沿用现有等宽只读文本控件，支持纵向/横向滚动和文字选择；复制文本与 TXT 导出使用完整 `ReportText`。`AnalysisData.Table` 仍是原始七项表，以保留赛德尔柱状图和既有表格消费者契约；**CSV 数据导出目前仍只含该原始表，不等同于完整 TXT 报告**。没有改动全局主题、其他分析图或安装/打包脚本。

## 数值定义与修正

换算依据为 [Ansys OpticStudio 官方 Seidel Coefficients 说明](https://ansyshelp.ansys.com/public/Views/Secured/Zemax/v252/en/OpticStudio_User_Guide/OpticStudio_Help/topics/Seidel_Coefficients.html)。Core 长度为 mm；以下 λ 使用 mm，n′ 和 u′ 分别为所选波长下最终像空间折射率和近轴边缘光线斜率。

- 逐面和累计值以未舍入的 `double` 换算，最后统一显示六位小数，不能先从显示表读回六位数再换算。
- 波前系数依次为 S1/(8λ)、S2/(2λ)、S3/(2λ)、S4/(4λ)、S5/(2λ)、CL/(2λ)、CT/λ。
- 横向单色换算公因子为 −1/(2n′u′)，轴向单色换算公因子为 1/(2n′u′²)；十列对应的组合依次是 S1、S2、3S2、2S3、S4、S3+S4、3S3+S4、S5 和两个色差项。
- 横向色差为 CL/(n′u′)、CT/(n′u′)，轴向色差为 CL/(n′u′²)、CT/(n′u′²)，不能机械地对全部轴向列套用单色符号规则。
- W220S/M/T 分别为 (S3+S4)/(4λ)、(2S3+S4)/(4λ)、(3S3+S4)/(4λ)。
- S5 移除原有多余负号，使用与 `(Ā/A)(S3+S4)` 等价的无除法表达式，避免 A=0 时错误置零。
- CL/CT 使用最短、最长已定义波长相对于所选波长折射率的相对色散变化；移除原有多余曲率因子。平面折射界面不应因此丢失色差贡献。
- 佩兹伐半径改为 `−1 / (n′ × Σ[c(n_after−n_before)/(n_before n_after)])`。
- 像方边缘光线斜率为零或无效时，横向/轴向换算显示 `—` 并注明未定义；不伪造零值。

## 2026-09-05 验证与适用范围

- `Captured123456ReportMatchesAllFourCoefficientTablesAtCapturedWavelengthTwo` 已恢复执行并通过。导入固定 `123456.ZMX`、显式选择波长 2 后，四张逐面和累计表按原 1e−6 容差端到端比较；未修改基准、处方或容差。
- 根因是旁轴报告误用真实光线的实像高转换，以及反向单位主光线遗漏物方传播。现使用前向旁轴映射求指定像高，RealImageHeight/ParaxialImageHeight 采用相同的线性共轭含义。纯 X/Y 旋转对称视场回归通过。
- 物方/像方主光线斜率约 +0.418772303751 / +0.085656480929，佩兹伐半径约 −376.28503335 mm。历史 −376.1420 及主光线符号失败已解决。
- 原始打印输入的舍入传播换算测试保留；它与新的端到端测试是不同证据。MS-L7 截图选中行仍有独立测试。
- 默认 App 和主解决方案已重建为 0 警告、0 错误，原输出锁定不再阻塞。完整测试结果见 [项目修复报告](PROJECT_REPAIR_2026-09-05.md)。本轮未重新进行真实 GUI 截图或安装包验收，先前截图验证仅作历史记录。

本次不新增圆锥/非球面的三阶贡献实现，不宣称官方支持面型已在 Workbench 全部对齐。CSV 仍只导出原始七项表，完整报告使用 TXT。
