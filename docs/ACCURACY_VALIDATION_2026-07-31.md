# 精度与页面对比验证（2026-09-05 更新）

默认精度权威是仓库固定的 Zemax OpticStudio 2026 R1 `123456.ZMX` 捕获基准。它只限定该文件、所保存的设置和该版本；Core 通用默认值、应用预设和捕获设置相互独立。Optiland 0.5.8 已有数据仅在 validation/history 作为冻结辅助历史回归，不再新增对照。后续外部验证以已提交的 Zemax OpticStudio 2026 R1 基准为主；每项必须记录镜头文件、设置、版本、单位、采样和容差。

## 当前验证

独立 [C# Zemax 比较工具](ZEMAX_COMPARISON_TOOL.md) 的实际捕获单独记录。下述当前构建/测试数量已包含新工具；再下方的数值不变性和旧基准对照仍是专用格式移除时的历史结果，不回写，也不将开发镜头测试当作用户指定 MS-L7 文件的首轮结论。

2026-09-06 Huygens MTF 后处理修复后的正式解决方案 Release 默认输出构建 `0` 警告、`0` 错误，完整主测试 `1233/1233`、工具测试 `104/104`，合计 `1337` 项通过，零失败、零跳过；锁定依赖从本地缓存还原，格式检查和 git diff --check 通过。MS-L7 重新执行全部 72 项，结果为 44 Pass、6 Close、2 Difference、17 Incomparable、3 Skipped，0 执行错误；主基准独立复验三项 Huygens MTF 均 Pass。新增 14 项回归包含原生 PSF 后处理重建，不能代替全链条精度结论。视场 MTF 的一处分量由 Pass 变为 Close，局部退步及未解决误差监控预算已明确记录，正式数值容差不变。未执行在线漏洞审计、独立实验室、旧外部报告工具、GUI 截图或安装包实测。当前证据见 `ZEMAX_HUYGENS_REPAIR_2026-09-06.md`，以下更早的移除审计仅作历史记录。详见 [移除审计与验证](PYTHON_OPTILAND_REMOVAL.md)。

最终重算 72 项：69 项有效结果、1 项不可用、2 项不适用、0 项异常。30 个历史映射条目中，29 项取得数据，26 项通过高度一致门禁，3 项坐标范围不一致；独立 Distortion 已从目录退场，1 项明确未比较，另有 2 个非等价映射排除。赛德尔另有严格端到端测试，不计入该映射数量。详见 [逐项结果与未对齐范围](PROJECT_REPAIR_2026-09-05.md#zemax-对齐边界)。

补充组只将三项输出范围设置为捕获坐标元数据，**29/29 可比映射高度一致，0 个数值/坐标门禁失败**。该组重算三项、复用同程序集且设置未变的其余结果；自动范围组的三项差异仍保留。范围覆盖和 PSF 峰值归一化形状比较的限定见[补充验证](PROJECT_REPAIR_2026-09-05.md#显式捕获坐标范围的补充验证)。

赛德尔四张逐面及累计系数表已按原有 1e−6 容差端到端通过，原跳过项恢复执行。新的误差门禁使用类型化单位、物理坐标交集、覆盖率、必需系列匹配及最差系列误差；不会按数组序号拉伸，也不根据数值选择翻转或转置。高度一致要求中位 NRMSE ≤ 3% 且最差 ≤ 10%，接近要求中位 ≤ 10% 且最差 ≤ 25%；坐标端点误差不得超过 5%，物理覆盖率至少 95%。

旧报告的 26 项高度一致、4 项接近是 2026-08-04 的历史分类。其 P90 门禁、坐标重采样与自动方向选择不足以排除误报，现不作为当前精度结论。旧捕获资料及历史报告原样保留。

`Centroid Sphere Wavefront`、`Best Fit Sphere Wavefront` 与 Zemax `WavefrontMap` 参考定义不等价，继续排除；已从公开目录退场的独立 `Distortion` 映射明确显示缺失，组合场曲/畸变另行比较。未映射的功能不能因成功输出 JSON 而计为 Zemax 数值通过。

基准完整性已只读验证通过：165 项中 148 项捕获、17 项不适用或失败；148 张图像含 106 张原生截图和 42 张标注的后备图。源 SHA-256 为 `0cd65a2f823baf5079f20f91d8310765899a182a6be72ddac53ede943f2bf75b`。完整性通过不等于当前精度通过。

本轮没有新的 OpticStudio/ZOS-API 实测或真实 Avalonia/Zemax GUI 截图验收。报告图片是当前 JSON 的离线重绘，不能替代界面验收。操作数导入和 MS-L7 的 MFE golden 仍是独立验证路径，未执行的兼容操作数与未捕获的新语义不计入分析一致性数量。

复现命令及本地报告路径见 [项目修复报告](PROJECT_REPAIR_2026-09-05.md#复现)。新运行输出放在 `artifacts/repair-2026-09-05/verified-recalculated`，不会覆盖不可改写的捕获基准目录。

## 本次架构移除后的重算

本次按同一固定镜头和原捕获设置完整重算 72 项，全部数值与移除前一致；71 份结果完整相同，另一份只改变不可用探测器的提示标签。26 项高度一致、3 项坐标范围不一致的外部比较分类保持不变。证据见 [移除审计](PYTHON_OPTILAND_REMOVAL.md) 和 [数值不变性记录](validation/PYTHON_REMOVAL_NUMERICAL_INVARIANCE_2026-09-05.json)。当前完整输出在 artifacts/python-removal-recalculated；前文修复报告与显式坐标范围补充组属于此前验证记录，本次未重新运行补充组或新建 Zemax 捕获。
