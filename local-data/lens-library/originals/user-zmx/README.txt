此目录是项目统一的 Zemax 输入根目录。

约定：
1. project/ 保存项目自带的示例和测试 ZMX，并由 Git 跟踪。
2. public/ 保存公开下载语料、manifest 和转换报告，由 Git 忽略。
3. 新增输入文件可直接放在本目录或自建子目录；后续转换会递归扫描。
4. 输入文件扩展名使用 .zmx 或 .ZMX。
5. 转换结果写入 STAROPT 示例库和应用镜头库，不覆盖这里的原始文件。
